{% import 'formalism/macros' as macros %}
{% spaceless %}

{% set defaults = {
    type: 'submit',
    attributesField: {
        'data-submit': '',
    },
} %}

{% set self = craft.formalism.deepMerge(defaults, params) %}
{{ craft.formalism.button(self) }}

{% endspaceless %}
