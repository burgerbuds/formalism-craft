{% import 'formalism/macros' as macros %}
{% spaceless %}

{% set defaults = {
    label: 'Label',
    id: 'switcher' ~ random(5000),
    baseClass: 'f-switcher',
} %}

{% set self = craft.formalism.deepMerge(defaults, params) %}
{{ craft.formalism.checkbox(self) }}

{% endspaceless %}
